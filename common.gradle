//version=5.36
ext{
    warName="${rootProject.name}.war"
    jarName="${rootProject.name}.jar"
    clientJarName="${rootProject.name}_client.jar"
    buildHibernateDir="${buildDir}/hibernate"
    nowDate = new Date();
    formattedDate=nowDate.format('MMM-dd-yyyy HH:MM');
    javaManifest=System.getProperty('java.version') + ' (' + System.getProperty('java.vendor') + ')'
    srcDir="$projectDir/src/main/java"
    rescDir="$projectDir/src/main/resources"
    webrootDir="$projectDir/src/main/webapp"
    sandboxDir="${buildDir}/sandbox"
    gnomex_version=5.37
    openSourceZipName="gnomex_tomcat_${gnomex_version}.zip"
    openSourceName="gnomex_${gnomex_version}"
    gnomexServerHome="c:/Program Files/Apache Software Foundation/Tomcat 7.0"
    gnomexDeployDir="webapps"
}
